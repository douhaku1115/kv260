vivado/
vivado_loop/
vivado_melody/
vivado_player/
vivado_stream/
simbuild/
*.jou
*.log
*.str
.Xil/
dfx_runtime.txt

# 音声データは第三者の録音由来のため配布しない
# (変換手順は audio/raw2hex.py にあるので各自の音源で再現できる)
audio/*.raw
audio/*.hex
audio/*.wav
audio/*.mp3

# 段12: 受信した放送の録音と掃引結果
#   放送内容そのものなので配布しない。掃引結果は場所ごとに違うので意味がない。
#   どちらも rtl_fm / rtl_power で各自の環境で取り直せる。
sw/*.raw
sw/*.csv

# ビルド生成物
#   ビットストリームは build_stream.tcl から再生成できるので置かない（1個 7.5MB）
simbuild_stereo/
*.bit
